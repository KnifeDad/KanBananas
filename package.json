{
  "name": "kanbananas",
  "version": "1.0.0",
  "description": "A fun and functional Kanban board application",
  "scripts": {
    "install-all": "cd server && npm install && cd ../client && npm install",
    "build": "cd server && npm run build && cd ../client && npm run build",
    "start": "cd server && npm start"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
